import 'antd/dist/antd.css';

// // 把loading方法挂载在全局React对象上，方便组件内直接调用
// React.loading = loading;
// React.close = close;
// // 方便类组件调用 内部直接使用this.loading即可
// React.Component.prototype.loading = loading;
// React.Component.prototype.close = close;
